{
  "built": "2026-03-03",
  "version": "2026.Q1",
  "tier_built": "all",
  "forced": false,
  "counts": {
    "councils": 228,
    "ranks": 7,
    "merit_badges": 142,
    "policies": 8
  },
  "notes": "merit_badges, ranks, policies, and councils content corrected 2026-08-02 — see docs/PLAYBOOK.md. Merit badges: all 142 files re-fetched after extract_content picked promotional widgets instead of real content on every page. Ranks: all 7 re-extracted after a PDF text-extraction bug produced doubled characters (e.g. 'LEADER' -> 'LLEEAADER') throughout every file, plus life.md had Eagle Scout's entire section wrongly appended (213 lines, should be 60) because the doubled-text bug broke the section-boundary regex; a second pass fixed readability (real GFM tables with accurate row counts, nested sub-lists, footnote formatting, blank-line-before-heading). Policies: all 7 re-verified — annual-health-medical-record.md had leftover empty lazy-load placeholder images stripped, and youth-protection-training.md had an embedded 'find your council' widget's raw JS/empty form labels stripped; the other 5 confirmed clean. Councils: the old zip-sampling approach (137 councils, a sample not a canvas) was replaced with a full pull from the authenticated organizations/councils API (228 councils) — found a 91-council gap plus 2 dissolved/merged councils and at least 2 renames the old approach couldn't have caught. address/zip/phone/website/email are null for all 228 records — that endpoint doesn't return them (a user-directed, explicit tradeoff for a clean rebuild over a partial merge). See fetch_councils_authenticated.py. built/version above still reflect the original 2026.Q1 full build; this was a targeted data-quality fix, not a quarterly refresh."
}